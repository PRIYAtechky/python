# Set data type example
# Sets store unique values (no duplicates) and are unordered

g = {100, 200, 300}
print(g)              # Prints the set
print(type(g))        # Prints the data type of g
