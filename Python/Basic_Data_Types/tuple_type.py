# Tuple data type example
# Tuples are similar to lists but cannot be changed (immutable)

f = (10, 20, 30)
print(f)              # Prints the tuple
print(type(f))        # Prints the data type of f
